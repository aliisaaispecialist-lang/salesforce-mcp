"""What the tools that only answer questions accept and return.

Split from the writes because that is the line everything else in this
connector already draws: the router lists one or the other, the tool
annotations declare it, and the approval gate applies to one side and not the
other. A reader asking "what can this thing change" should not have to open
seventeen files to find out.
"""
