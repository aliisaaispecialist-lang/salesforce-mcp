"""What each action accepts, what it returns, and how it fails.

One module per action, plus the shared vocabulary they are all written in. The
same declarations produce the MCP tool definition, the OpenAPI document, and
the documentation, so those three cannot drift apart.
"""
